import styled from "styled-components";

const BadgeStyled = styled.div`
  position: absolute;
  top: 7.5%;
  right: 7.5%;
  text-transform: uppercase;

  background: rgba(249, 81, 110, 0.8);
  color: #fff;
  /* transform: rotate(-40deg); */
  text-align: center;
  /* width: 12rem; */
  padding: 0.5rem;
  border-radius: 30px;
  font-weight: 600;
  font-size: 0.8rem;
`;

export { BadgeStyled };
